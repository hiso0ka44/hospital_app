abstract class RegisterNavigator {
  // kol el actions hena m3molha abstract ( elly been el view w el view model)
  // 3bara 3n interface el (view / view model) y2dro ywslolo
  void showMyLoading();

  void hideMyLoading();

  void showMessage(String message);
}
